// 10 Pro-Level Recursion Challenges

// Write a recursive function to flatten deeply nested arrays.

// Build a recursive function that prints all paths in a nested object.

// Implement a recursive binary search.

// Compute the greatest common divisor (GCD) recursively.

// Create a recursive function that generates all permutations of a string.

// Write a recursive power function (xⁿ).

// Create a recursive function that checks if a string is a palindrome.

// Write a recursive directory walker (simulated in JS object).

// Recursively calculate the depth of a nested object/array.

// Use recursion to render a tree-like HTML structure from nested JSON.